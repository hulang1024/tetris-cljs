{:deps true
 :dev-http {9000 [".", "public"]}
 :builds
 {:app {:target :browser
        :output-dir "."
        :asset-path "/js"
        :runtime :browser

        :modules {:main {:entries [tetris.main]
                         :init-fn tetris.main/init}}

        :dev {:compiler-options {:warnings {:infer-warning true}}
              :modules {:main {:entries [tetris.main]
                               :init-fn tetris.main/init
                               :preloads [tetris.dev-preload
                                          malli.dev.cljs-kondo-preload]}}}

        :release {:compiler-options {:optimizations :advanced
                                     :externs ["externs.js"]
                                     :infer-externs true}}

        :devtools {:preloads [tetris.dev-preload]}}}
 :nrepl {:port 7888}}
